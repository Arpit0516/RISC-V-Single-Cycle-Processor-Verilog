# RISC-V-Single-Cycle-Processor-Verilog
A 32-bit single-cycle RISC-V processor designed in Verilog HDL implementing the RV32I base integer instruction set (partial). The processor includes instruction fetch, decode, execute, memory access, and write-back stages with a hardcoded instruction memory for functional verification.
